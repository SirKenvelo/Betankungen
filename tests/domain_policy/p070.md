# P070
**Stand:** 2026-02-24

Delete-Guard-Policy fuer `cars`: Loeschen ist verboten, wenn `fuelups` auf das Fahrzeug referenzieren.

Golden-Template-Dateien:
- `fixtures/p070_base.sql`
- `cases/t_p070__01__cars_delete_blocked_by_fuelups.sh`

Erwartung fuer Cases:
- `--delete cars --car-id <id>` mit Referenzen liefert Exitcode `!= 0`.
- Fehler enthaelt `P-070`.
- Kein Prompt darf erscheinen (Guard greift vor interaktiver Loeschbestaetigung).
- Datenbestand bleibt unveraendert (kein stilles Durchrutschen).
