# P061
**Stand:** 2026-03-13

Cost-Stats-Policy: Car-/Period-Scope muessen strikt wirken (keine Cross-Scope-Leaks).

Golden-Template-Dateien:
- `fixtures/p061_cost_scope_base.sql`
- `cases/t_p061__01__cost_scope_isolation.sh`

Erwartung fuer Cases:
- `--stats cost --car-id <id>` aggregiert nur das gewaehlte Fahrzeug.
- Kombination mit Period-Scope (`--from/--to`) reduziert die Basis strikt auf den Zeitraum.
- Scope-Label (`Scope`, `Period filter`) spiegeln den aktiven Scope korrekt wider.
